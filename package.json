{
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "npm run build && touch dist/.nojekyll && npx gh-pages -d dist --dotfiles"
  },
  "dependencies": {
    "@astrojs/mdx": "^8.0.0",
    "astro": "^7.3.1",
    "gsap": "^3.15.0",
    "three": "^0.184.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  },
  "name": "portfolio",
  "private": true,
  "type": "module"
}
